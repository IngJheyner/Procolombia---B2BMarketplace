(function ($, Drupal) {
    'use strict';
    // **********************
    // *** Call functions ***
    // **********************
    Drupal.behaviors.cp_edit_international_company = {
        attach: function (context, settings) {

            //init
            if (context == document) {
                //init();
            }
        }
    };


}(jQuery, Drupal));
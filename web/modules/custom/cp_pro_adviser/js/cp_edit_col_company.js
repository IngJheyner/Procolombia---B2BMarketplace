(function ($, Drupal) {
    'use strict';
    // **********************
    // *** Call functions ***
    // **********************
    Drupal.behaviors.cp_edit_col_company = {
        attach: function (context, settings) {

            //init
            if (context == document) {
                //init();
            }
        }
    };


}(jQuery, Drupal));